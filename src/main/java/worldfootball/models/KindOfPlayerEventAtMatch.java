package worldfootball.models;
// befor use this enum , we want to know all kinds in this site
// or u want to add throw run time exception if the project find a new kind
public enum KindOfPlayerEventAtMatch {

    yellowCard,
    redCard,
    substituteIn,
    substituteOut
}
